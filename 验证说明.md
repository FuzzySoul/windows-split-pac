# WindowsSplitPAC 验证说明

本文件夹是完整项目 + **可直接运行的 Windows 程序**。

## 🚀 快速开始（入口）
- **双击 `双击打开GUI.cmd`** → 自动打开图形界面（如果 exe 不存在会自动先编译）。
- 或者直接双击 **`app\windows-split-pac-gui.exe`** 运行。
- 界面里点「启用智能分流」/「保存并应用」即可整体使用，会直接作用于线上 `C:\proxy` 并重新生成 PAC（一键开关、三桶/高手两种规则模式、规则清单、诊断）。
- 改规则后**无需重启**（服务实时读 PAC）；界面上仍有「重启分流服务」可选按钮。
- VM/隔离环境测试：`powershell -File .\scripts\Test-Isolated.ps1`（临时目录+非8765端口，不碰注册表/线上）。

> 包内已附带编译好的 Windows x64 程序（`app\windows-split-pac-gui.exe`，31MB）。
> 如果你的电脑是 x86 32 位，需要重新用源码构建（见下）。

## 0. 前提（仅当需要从源码构建时）
- Windows 10/11 + PowerShell 5.1+
- Rust 工具链（rustup + stable）
- Python 3 + genpac（`pip install -r requirements.txt`；你的机器已经装好了）

## 1. 一键全量测试（推荐先跑）
在 **Windows** 的 PowerShell 里，进入本文件夹：
```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\Test-All.ps1
```
它会依次验证：
1. 所有 PowerShell 脚本语法
2. Rust core 单测（11 个）
3. Rust workspace 编译检查
4. 线上真实服务身份（只读）：/healthz、pid 匹配、Windows 在用我们的 PAC
5. Apply 干跑计划里包含全部 5 步（只读）

如果你的机器上没有跑这套 PAC 服务，加 `-SkipLive` 跳过第 4/5 步：
```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\Test-All.ps1 -SkipLive
```

## 2. 从源码跑 GUI（用不到 exe 时）
```powershell
cd .\rust-gui
cargo run
```
应看到：
- 顶部「一键启用 / 停止 / 刷新」
- 四个状态卡片
- 「当前服务识别」横幅（显示真实 serve_pac / PID）
- 「诊断（高级）」抽屉（服务/healthz/PID/Windows 是否在用/自启/规则同步）
- 「自定义规则与诊断」里的三桶规则编辑：🌐 强制走代理 / 🏠 强制直连 / ⚡ 智能(GFWList)

## 3. 手动验证线上（只读）
```powershell
.\scripts\Get-ServiceIdentity.ps1
```
看 JSON：
- `server_kind` = `real_serve_pac`
- `healthz_ok` = `true`
- `pid_file_matches` = `true`
- `windows_using_our_pac` = `true`

## 4. 如果要真正“一键应用”（会写文件/注册表）
```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\Apply-PacConfig.ps1 -Apply
```
- 会备份当前 PAC/规则（`C:\proxy\*.bak`）和 Windows 代理设置（`data\windows-proxy-backup.json`）
- 重新生成 `C:\proxy\proxy.pac`（genpac + GFWList + 你的 5 条规则）
- 复用/重启服务、刷新 WinINET
- 失败自动回滚
不放心就先不带 `-Apply` 跑一次干跑（只打印计划，什么都不写）。

## 5. 备注
- 线上引擎已升级为产品版（带 /healthz、自写 pid），旧版备份在
  `C:\Users\62003\.hermes\serve_pac.py.pre-healthz.bak`。
- 本沙箱已验证：core 11 测、rustfmt、Windows 目标 cargo check、Windows 目标 clippy -D warnings、
  线上 healthz/pid/5条规则、Apply 干跑 5 步 —— 8/8 通过；并已交叉编译出 Windows GUI exe。
